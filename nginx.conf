worker_processes auto;

events {
    worker_connections 1024;
}

http {

    upstream dev {
        server development:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://dev;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}